import MainTitle from "./MainTitle";

const Messages = () => {
    return ( 
        <div>
            <MainTitle title={"..."} subTitle={"...."} />
        </div>
     );
}
 
export default Messages;